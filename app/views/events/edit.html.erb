<div class="container py-4">
  <div class="card">
    <div class="card-header">
      <h1 class="h4 mb-0"><%= t('events.edit_event') %></h1>
    </div>
    <div class="card-body">
      <%= render 'form', event: @event %>
      <div class="mt-3">
        <%= link_to t('events.back_to_list'), events_path, class: 'btn btn-outline-secondary' %>
      </div>
    </div>
  </div>
</div>
