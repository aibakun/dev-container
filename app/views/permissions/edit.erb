<div class="container py-4">
  <div class="card">
    <div class="card-header">
      <h1 class="h4 mb-0"><%= t('.title') %></h1>
    </div>
    <div class="card-body">
      <%= render 'form', permission: @permission %>
      <div class="mt-3">
        <%= link_to t('permissions.actions.back'), permissions_path, class: 'btn btn-outline-secondary' %>
      </div>
    </div>
  </div>
</div>
